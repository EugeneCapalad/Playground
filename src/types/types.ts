export interface AppDetails {
    id: number,
    name : string,
    status: string,
    logo: string
}

export interface TaskType {
    id: number,
    taskName: string,
    dateCreated: string,
    status: number
}